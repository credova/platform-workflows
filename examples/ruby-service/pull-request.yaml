# Example: Ruby service with container build
name: Pull Request
on:
  pull_request:
    branches: [master]
permissions:
  contents: read
  pull-requests: write
jobs:
  ci:
    uses: credova/platform-workflows/.github/workflows/ruby-pull-request.yaml@v1.0.3
    secrets: inherit
